const theme = {
  background: "#000",
  borderRadius: "50%",
  borderColor: "#BF67AD",
  typography: {
    color: "#111",
    fontFamily: `"Heebo", Arial, Helvetica, sans-serif`
  }
};

export default theme;
